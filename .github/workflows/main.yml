name: Windows Build

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-11-arm

    permissions:
      contents: write
    steps:
      - name: Check system arch
        run: wmic os get osarchitecture
      
